<Window
    x:Class="RTXVideoConverter.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="RTX Video Converter">

    <Grid x:Name="AppRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="AppTitleBar"
            MinHeight="80"
            Background="{ThemeResource TitleBarBackgroundBrush}"
            IsBackButtonVisible="False"
            IsPaneToggleButtonVisible="False">
            <TitleBar.LeftHeader>
                <Grid ColumnDefinitions="74,Auto" Translation="0,3,0">
                    <Button
                        Width="40"
                        Height="40"
                        Padding="0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="PaneToggleButton_Click"
                        AutomationProperties.Name="展开或收起导航"
                        ToolTipService.ToolTip="导航">
                        <FontIcon Glyph="&#xE700;" FontSize="18" />
                    </Button>
                    <StackPanel Grid.Column="1" Margin="18,0,0,0" Orientation="Horizontal" Spacing="12">
                        <Image Width="36" Height="36" Source="Assets/app-icon.png" Stretch="Uniform" />
                        <TextBlock
                            Text="RTX Video Converter"
                            FontFamily="{StaticResource AppDisplayFontFamily}"
                            FontSize="23.5"
                            FontWeight="SemiBold"
                            Foreground="{ThemeResource TextHeadingBrush}"
                            VerticalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </TitleBar.LeftHeader>
        </TitleBar>

        <NavigationView
            x:Name="RootNavigationView"
            Grid.Row="1"
            PaneDisplayMode="LeftCompact"
            CompactPaneLength="74"
            OpenPaneLength="240"
            IsPaneOpen="False"
            IsPaneToggleButtonVisible="False"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            IsTitleBarAutoPaddingEnabled="False"
            SelectionFollowsFocus="Disabled"
            ItemInvoked="RootNavigationView_ItemInvoked">
            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                <StaticResource x:Key="NavigationViewDefaultPaneBackground" ResourceKey="NavigationLayerBrush" />
                <StaticResource x:Key="NavigationViewExpandedPaneBackground" ResourceKey="NavigationLayerBrush" />
            </NavigationView.Resources>
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="HomeNavigationItem" MinHeight="46" Margin="0,-5,0,21" Content="转换" Tag="home">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Margin="0,0,0,22" Content="任务记录" Tag="history">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE81C;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="SettingsNavigationItem" Content="设置" Tag="settings">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Border Background="{ThemeResource AppLayerBrush}" CornerRadius="16,0,0,0">
                <ContentPresenter x:Name="PageHost" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                    <ContentPresenter.ContentTransitions>
                        <TransitionCollection>
                            <ContentThemeTransition HorizontalOffset="8" />
                        </TransitionCollection>
                    </ContentPresenter.ContentTransitions>
                </ContentPresenter>
            </Border>
        </NavigationView>

    </Grid>
</Window>
